# Minimal dependencies for running unit tests
# These are the core dependencies needed for imports to work

# Core dependencies
click>=8.0.0
pyyaml>=6.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
